apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: payment-api
  namespace: payments
  labels:
    app.kubernetes.io/name: payment-api
    app.kubernetes.io/component: api
    nucleus.io/tier: "0"
spec:
  replicas: 4
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: payment-api

  strategy:
    canary:
      # No traffic router configured, so Rollouts approximates traffic split
      # by replica ratio: 25% weight with 4 replicas = 1 canary pod.
      # A real setup uses an ingress or mesh for precise weighting - noted
      # as a limitation, not a design choice.
      steps:
        - setWeight: 25
        - pause: {duration: 90s}
        - setWeight: 50
        - pause: {duration: 90s}
        - setWeight: 100

      # Background analysis: runs continuously from step 1 until the rollout
      # finishes, so a regression appearing at ANY weight aborts. A step
      # analysis would only check at one moment and needs an explicit count.
      analysis:
        templates:
          - templateName: success-rate
        startingStep: 1


  template:
    metadata:
      labels:
        app.kubernetes.io/name: payment-api
    spec:
      terminationGracePeriodSeconds: 45
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: api
          image: nucleus/payment-api:0.2.0-bad
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: APP_VERSION
              value: "0.2.0-bad"
            - name: INJECT_ERROR_RATE
              value: "0.25"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests: {cpu: 50m, memory: 96Mi}
            limits: {cpu: 200m, memory: 96Mi}
          startupProbe:
            httpGet: {path: /readyz, port: http}
            periodSeconds: 2
            failureThreshold: 15
          readinessProbe:
            httpGet: {path: /readyz, port: http}
            periodSeconds: 3
            failureThreshold: 2
          livenessProbe:
            httpGet: {path: /healthz, port: http}
            periodSeconds: 10
            failureThreshold: 3
          lifecycle:
            preStop:
              sleep: {seconds: 5}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
